<resources>
    <!-- Theme overlay used on views (if any view uses android:theme="@style/ThemeOverlay.Iskorly") -->
    <style name="ThemeOverlay.Iskorly" parent="@style/ThemeOverlay.Material3">
        <!-- Force text to render black on surface where labels might inherit colorOnSurface -->
        <item name="colorOnSurface">@android:color/black</item>
        <item name="android:textColor">@android:color/black</item>


    </style>
</resources>