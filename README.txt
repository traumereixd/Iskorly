Item Score Analyzer v1.0
Purpose: Scan answer sheets, OCR, parse answers, score, export CSV.
Permissions: Camera (capture), Internet (send image to Vision for text extraction).
No tracking, data stored locally. API key not bundled in this release (user will enter on first run).